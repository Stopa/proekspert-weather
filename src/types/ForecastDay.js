// @flow

export type ForecastDay = {
  date: number,
  conditionCode: number,
  maxTempC: number,
  maxTempF: number,
  minTempC: number,
  minTempF: number,
  avgTempC: number,
  avgTempF: number,
}
